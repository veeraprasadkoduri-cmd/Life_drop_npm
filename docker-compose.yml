services:
  mongo:
    image: mongo:7
    container_name: lifedrop-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  backend:
    image: veeradockerhub/lifedrop-backend:latest
    container_name: lifedrop-backend
    restart: always
    depends_on:
      - mongo
    environment:
      MONGO_URI: mongodb://mongo:27017/lifedrop
      JWT_SECRET: your_super_secret_key_here
      PORT: 5000
      NODE_ENV: production

  frontend:
    image: veeradockerhub/lifedrop-frontend:latest
    container_name: lifedrop-frontend
    restart: always
    depends_on:
      - backend

  nginx:
    image: nginx:1.25-alpine
    container_name: lifedrop-nginx
    restart: always
    depends_on:
      - frontend
      - backend
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  mongo_data:
